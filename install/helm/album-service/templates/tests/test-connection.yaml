apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "album-service.fullname" . }}-test-connection"
  labels:
    {{- include "album-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "album-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
